import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Log in — Getstaked",
  description: "Log in to your Getstaked account.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
