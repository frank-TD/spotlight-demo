import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Create account — Getstaked",
  description: "Create your Getstaked account to fund, create, and distribute AI films.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
